name: Anonymous

# Logging settings
logging:
  level: warn
  loggers:
    "nl.knaw.huygens.pergamon": trace

# Server settings
server:
  applicationConnectors:
    - type: http
      port: 0
  adminConnectors:
    - type: http
      port: 0
  requestLog:
    appenders: []
